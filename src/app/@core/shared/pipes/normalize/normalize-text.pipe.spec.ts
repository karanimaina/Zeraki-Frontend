import { NormalizeTextPipe } from './normalize-text.pipe';

describe('NormalizeTextPipe', () => {
  it('create an instance', () => {
    const pipe = new NormalizeTextPipe();
    expect(pipe).toBeTruthy();
  });
});
